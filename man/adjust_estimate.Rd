% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_estimate.R
\name{adjust_estimate}
\alias{adjust_estimate}
\title{Adjust estimates for linkage bias using a validation variable}
\usage{
adjust_estimate(
  weight_result,
  linked_data,
  population_data,
  outcome = NULL,
  validation_var,
  model_formula = NULL,
  coef_of_interest = NULL,
  model_fit = stats::lm,
  model_args = list(),
  extract_estimate = NULL,
  n_boot = 200,
  ci_level = 0.95
)
}
\arguments{
\item{weight_result}{An object of class "linkinfer_ipw", "linkinfer_poststrat" or "linkinfer_rake" containing adjustment weights.}
\item{linked_data}{Data.frame of the linked subsample (used to compute the weights). Must contain the outcome and validation variable.}
\item{population_data}{Data.frame of the full population. Used to compute the population‑level outcome estimate.}
\item{outcome}{Character string naming the outcome variable (if NULL, a model‑based workflow is used).}
\item{validation_var}{Character string naming the out‑of‑model validation variable (binary).}
\item{model_formula}{Formula object for model‑based adjustment (required when outcome is NULL).}
\item{coef_of_interest}{(Deprecated) kept for compatibility; not used.}
\item{model_fit}{Function to fit the model (default \code{stats::lm}).}
\item{model_args}{Additional arguments passed to \code{model_fit}.}
\item{extract_estimate}{Function that extracts the scalar estimate from a fitted model (required for model‑based workflow).}
\item{n_boot}{Number of bootstrap replications for standard‑error estimation.}
\item{ci_level}{Confidence level for intervals.}
}
\value{
A list of class "linkinfer_adjusted" containing:
\describe{
\item{pop_estimate}{Population (true) estimate of the outcome.}
\item{estimate}{Weighted estimate for the full linked sample.}
\item{sestimate}{Weighted estimate for the subset with a valid validation variable.}
\item{testimate}{Weighted estimate for the subset where the validation variable indicates a true match.}
\item{adjustment_factor}{Factor = testimate / sestimate.}
\item{aestimate}{Adjusted estimate = estimate * adjustment_factor.}
\item{se_adj}{Bootstrap standard error of the adjusted estimate.}
\item{ci_adj}{Confidence interval for the adjusted estimate.}
}
}
\description{\nThe function defaults to using \code{stats::lm} for model‑based adjustments, but any fitting function that accepts a `formula`, `data`, and a `weights` argument (e.g., \code{stats::glm}, \code{survival::coxph}, \code{lme4::lmer}) can be supplied via the `model_fit` argument. 
Provides bias‑adjusted estimates using inverse‑probability or post‑stratification weights together with a validation variable. Either a direct outcome mean/rate is adjusted, or a model‑based estimate (e.g., regression coefficient) is adjusted.}
\examples{
# Simple mean adjustment
weight_obj <- weight_ipw(population, linked_indicator = "linked", covariates = "age")
adj <- adjust_estimate(
  weight_result = weight_obj,
  linked_data = linked_sample,
  population_data = population,
  outcome = "race_diff",
  validation_var = "middle_initial_match",
  n_boot = 100
)
print(adj)

# Model‑based adjustment (regression coefficient)
model_form <- age_at_death ~ race
adj_mod <- adjust_estimate(
  weight_result = weight_obj,
  linked_data = linked_sample,
  population_data = population,
  outcome = NULL,
  validation_var = "middle_initial_match",
  model_formula = model_form,
  extract_estimate = function(fit) coef(fit)["raceBlack"],
  n_boot = 100
)
print(adj_mod)
}
