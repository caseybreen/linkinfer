% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/linked_data.R
\name{linked_data}
\alias{linked_data}
\title{Simulate linked census data}
\usage{
linked_data(n, p, v1, v2, seed = NULL)
}
\arguments{
\item{n}{Integer. Population size to simulate.}
\item{p}{Numeric in (0,1). Approximate proportion of the population that is successfully linked.}
\item{v1}{Numeric in (0,1). Proportion of linked observations that have a valid middle‑initial variable.}
\item{v2}{Numeric in (0,1). Among linked observations with a valid middle‑initial, the proportion that have a matching middle initial.}
\item{seed}{Optional integer. If supplied, the random number generator is set locally before adding stochastic noise to `linked_prob`.}
}
\value{
A list with two elements:
\describe{
\item{population}{Data frame of the full simulated population.}
\item{linked}{Data frame of the linked subsample. Includes variables `race_diff`, `middle_initial_match`, and `age_at_death`.}
}
}
\description{
Generates a synthetic census‑style population and a linked subsample with realistic linkage probabilities and validation variables. The function adds stochastic noise to linkage probabilities to make the simulated data useful for testing and vignette examples.
}
\examples{
# Simulate a small dataset
sim <- linked_data(n = 1000, p = 0.2, v1 = 0.5, v2 = 0.8)
head(sim$population)
head(sim$linked)
}
