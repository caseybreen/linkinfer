% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weight_ipw.R
\name{weight_ipw}
\alias{weight_ipw}
\title{Inverse probability weights for linked samples}
\usage{
weight_ipw(
  population,
  linked,
  covariates,
  method = c("logistic", "ml"),
  trim = c(0.01, 0.99),
  stabilize = TRUE,
  ranger_args = list(num.trees = 500)
)
}
\arguments{
\item{population}{A data.frame of the full population.}

\item{linked}{A data.frame of the linked subsample. Must contain the same
covariates as \code{population}.}

\item{covariates}{A character vector of covariate names used to model the
linkage probability. Must be present in both data.frames.}

\item{method}{Character. Either \code{"logistic"} (default) for logistic regression
or \code{"ml"} for a random forest via \code{\link[ranger:ranger]{ranger::ranger()}}.}

\item{trim}{Numeric vector of length 2 giving quantiles at which to
trim propensity scores. Default \code{c(0.01, 0.99)}.}

\item{stabilize}{Logical. If \code{TRUE} (default), use stabilized weights
\eqn{w_i = P(\text{linked}) / p_i} instead of \eqn{1/p_i}.}

\item{ranger_args}{A list of additional arguments passed to
\code{\link[ranger:ranger]{ranger::ranger()}} when \code{method = "ml"}. Default: \code{list(num.trees = 500)}.}
}
\value{
A list of class \code{"linkinfer_ipw"} with elements:
\describe{
\item{weights}{Numeric vector of adjustment weights for linked records.}
\item{propensity_scores}{Numeric vector of estimated propensity scores
for all records (population rows, then linked rows).}
\item{model}{The fitted model object (glm or ranger).}
\item{method}{Character string indicating the method used.}
\item{n_population}{Number of rows in the population.}
\item{n_linked}{Number of rows in the linked sample.}
\item{covariates}{The covariates used.}
}
}
\description{
Estimates the probability that each record appears in the linked sample
and returns inverse probability weights to adjust for selective linkage
(missed matches). Implements the weighting approach described in Equation 2
of Breen and Joo (2026): \eqn{w_i = 1 / p_i}.
}
\examples{
data(census_population)
data(linked_sample)
result <- weight_ipw(
  population = census_population,
  linked = linked_sample,
  covariates = c("age", "sex", "race", "education", "urban")
)

}
